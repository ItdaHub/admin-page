import styled from "styled-components";

export const VisitorChartStyled = styled.div`
  &.visitor-wrap {
    padding: 24px;
    .visitor-title {
      margin: 25px 0;
      font-weight: 700;
      font-size: 17px;
    }
    .visitor-chart {
      margin-top: 40px;
    }
  }
`;
