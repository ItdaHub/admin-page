import styled from "styled-components";

export const VisitorChartStyled = styled.div`
  &.visitor-wrap {
    .visitor-title {
      margin: 25px 0;
      font-weight: 700;
      font-size: 17px;
    }
    .visitor-chart {
      margin-top: 40px;
    }
  }
`;
